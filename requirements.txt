Flask
Flask-SQLAlchemy
psycopg2-binary
flask-socketio
eventlet
redis
gunicorn

# 🔐 security & env
python-dotenv

# 🔑 password hashing (already werkzeug এ আছে, but optional upgrade)
bcrypt

# ⚡ background job (future use)
celery

# 🧠 migration (VERY IMPORTANT)
Flask-Migrate
